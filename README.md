# scala-tree
A recursive implementation of an N-ary tree, in Scala

The interesting files are `Tree.scala`, which contains the implementation and `TreeSpec.scala`, which contains the tests. The other files are just Scala project scaffolding.
